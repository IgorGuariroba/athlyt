# Histórico da memória

## 2026-07-30

- Criada [rebuild-restart-apos-mudanca.md](rebuild-restart-apos-mudanca.md): a aplicação acessada pelo Tailscale Funnel roda como build de produção; registrado que toda mudança deve terminar com `npm run build` e reinício por `npm start` na porta 3000. Fonte: instrução do responsável pelo ambiente.
- Criada [ci-lockfile-npm-runner.md](ci-lockfile-npm-runner.md): `npm ci` falhou em todos os jobs na primeira execução real do CI, e a correção do lock feita com o npm local não surtiu efeito porque o runner usa outra versão. Registrada por ser um erro caro de diagnosticar e fácil de repetir. Fonte: PR #35.
- Criada [mudanca-ui-atualiza-e2e.md](mudanca-ui-atualiza-e2e.md): o redesenho das etapas de peso e objetivo alterou o contrato acessível sem atualizar a jornada E2E, que falhou procurando os controles antigos. Registrada para que futuras mudanças de UI incluam a manutenção e execução dos testes afetados no mesmo conjunto. Fonte: PR #38.
- Criada [e2e-auth-url-local.md](e2e-auth-url-local.md): ao validar as réguas de altura e peso, os cenários E2E autenticados falhavam na primeira asserção por redirecionamento à raiz. A causa era o `AUTH_URL` do `.env` apontando para o Tailscale, que faz o Auth.js exigir cookie `__Secure-` incompatível com o helper de seed em HTTP. Registrada por levar a suspeitar do componente errado. Fonte: sessão de validação da roleta.
- Criada [limpeza-pos-merge.md](limpeza-pos-merge.md): após o merge do PR #38, o repositório local permaneceu na branch concluída e com a `main` desatualizada. Registrado o encerramento obrigatório do ciclo com sincronização por avanço rápido e remoção segura das branches local e remota. Fonte: PR #38.

## 2026-08-01

- Criada [estado-offline-fora-do-react.md](estado-offline-fora-do-react.md): ao implementar o outbox offline (issue #21), três reescritas sucessivas não satisfizeram `react-hooks/set-state-in-effect` porque o modelo estava errado — a fila em IndexedDB e a conectividade não são estado do componente. Registrada porque o mesmo padrão vai reaparecer no Diário e no Check-in, e porque a janela de inconsistência é invisível em teste manual. Fonte: issue #21.
- Criada [persistencia-visivel-apos-retorno.md](persistencia-visivel-apos-retorno.md): terceira ocorrência do relato "não persistiu" — depois de dois casos na triagem (#49 e #50), o cartão "Treino do dia" do Início voltou a oferecer o treino recém-concluído. Registrada porque as três causas são distintas (escrita, invalidação e derivação) e o padrão comum é o teste terminar na tela de sucesso sem voltar por navegação real ao ponto de partida. Fontes: commits 5f0ed54, 2c5eefd e 29f0d78.

## 2026-08-02

- Criada [protocolo-de-coleta-custa-friccao.md](protocolo-de-coleta-custa-friccao.md): ao redesenhar a Avaliação Corporal Inicial, o protocolo de duas leituras por circunferência produziu seis campos na triagem e trinta na tela de proporções; o usuário rejeitou o esforço pedido e o protocolo virou `fita-v2` (uma leitura). Registrada porque duas tentativas anteriores de corrigir pela UI não tocaram a causa, e porque a mudança exigiu percorrer domínio, cinco pontos das specs, um ADR novo e três telas. Fonte: ADR 0007.
- Criada [upload-em-server-action-estoura-mudo.md](upload-em-server-action-estoura-mudo.md): o relato de que "Enviar para storage privado" e "Continuar" não faziam nada na tela de fotos apontava para o redesenho recém-feito, mas a causa era o limite de 1 MB das Server Actions rejeitando o corpo com 413 antes de a action rodar. Registrada porque o sintoma incrimina a UI errada, o erro não deixa rastro no navegador e outras telas do produto ainda vão receber upload. Fonte: sessão de 2026-08-03.
- Atualizada [rebuild-restart-apos-mudanca.md](rebuild-restart-apos-mudanca.md): ao redesenhar a revisão de treino, a URL do Tailscale continuou servindo a versão antiga mesmo após `npm run build` (chunk de rota reaproveitado) e o `npm start` disparado pelo agente morria com a sessão do comando. Acrescentadas as duas verificações — limpar `.next` e conferir com `grep` no build; subir com `setsid` e validar por `curl`. Fonte: sessão de 2026-08-07.
- Criada [aritmetica-de-dias-locais.md](aritmetica-de-dias-locais.md): ao validar manualmente a persistência do Diário (issue #22), o botão "Dia anterior" saltou de 02/08 para 31/07 — a folga de ±26h somada à meia-noite local para absorver horário de verão atravessava a fronteira do dia anterior. Registrada porque a mesma aritmética reaparece em Check-in, Cadência Adaptativa, Revisão Semanal e Progresso, e porque 238 testes verdes não pegaram o defeito: nenhum deles andava entre dias. Fonte: PR #57.

## 2026-08-07

- Criada [tokens-de-tipografia-e-tailwind-merge.md](tokens-de-tipografia-e-tailwind-merge.md): ao revisar a tela de Progresso contra o MacroFactor Visual System, dois defeitos silenciosos respondiam pela maior parte da divergência — `text-title-lg` não existe na escala e caía em 16px, e o `cn` descartava `text-primary-foreground` ao mesclar com `text-label-lg`, deixando o CTA branco com texto branco. Registrada porque nenhum dos dois produz erro de build, lint ou tipo, e porque a correção pertence ao `cn`, não às páginas. Fonte: revisão da tela de Progresso.
- Criada [dev-nao-sobe-infraestrutura.md](dev-nao-sobe-infraestrutura.md): o job E2E do PR #61 falhou em `config.webServer was not able to start` porque `npm run dev` passou a chamar `db:up` — o Playwright usa `dev` como webServer e o CI já fornece Postgres como service. O mesmo PR expôs um `compose.yml` com volume `external` apontando para o volume anônimo de uma máquina. Registrada porque a conveniência local muda o contrato de um script compartilhado e a falha aparece longe da causa. Fonte: PR #61.
- Criada [cron-em-container-perde-o-path.md](cron-em-container-perde-o-path.md): ao montar o serviço de backup do compose de produção, o job agendado não disparava e, quando passou a disparar, o `mc` estava fora do PATH do cron — o dump seria gerado e o upload falharia todo dia às 03:00. Registrada porque o serviço sobe saudável e a falha só existe dentro da janela agendada, invisível para quem valida executando o script à mão. Fonte: validação do backup contra MinIO local.
- Atualizada [ci-lockfile-npm-runner.md](ci-lockfile-npm-runner.md): o `EUSAGE` reincidiu no PR #61 e custou três rodadas de CI por três armadilhas ausentes do registro original — validar com `--ignore-scripts` mascara o defeito, `install --package-lock-only` não cria entradas ausentes (foi preciso apagar e regenerar o lock) e `npm install` com o npm local desfaz a correção antes do commit. Fonte: PR #61.
- Atualizada [rebuild-restart-apos-mudanca.md](rebuild-restart-apos-mudanca.md): o procedimento manual virou `npm run app:up` / `app:down` (scripts/app-local.sh). A adoção de `output: "standalone"` invalidou `next start` e trouxe duas armadilhas novas — o standalone não copia `.next/static` nem `public` (página abre sem CSS) e infere a raiz por lockfiles acima do projeto, o que aninhou a saída por causa de um `package-lock.json` no home; `outputFileTracingRoot` fixa a raiz. Fonte: sessão de subida local em modo produção.

## 2026-08-08

- Criada [porta-publicada-colide-com-o-proxy.md](porta-publicada-colide-com-o-proxy.md): o primeiro deploy no Dokploy falhou em `port is already allocated` depois de o banco subir e as migrações rodarem — o `ports` do compose de produção disputava a 3000 com o painel, que é quem ocupa a porta no host. Registrada porque a falha chega tarde, com o estado meio construído, e porque o `ports` é sempre indevido sob um proxy que fala com o container pela rede interna. Fonte: PR #67.

## 2026-08-10

- Criada [alias-de-servico-colide-em-rede-compartilhada.md](alias-de-servico-colide-em-rede-compartilhada.md): logo após o primeiro deploy do Athlyt, o login de outro serviço da VPS passou a falhar de forma intermitente com `28P01`. A causa não era conflito de porta (hipótese inicial), e sim o serviço `db` do Athlyt colidindo com o `db` do outro projeto na `dokploy-network`: o DNS do Docker resolvia o alias para dois IPs em round-robin e o app vizinho autenticava contra o Postgres errado. Serviço renomeado para `athlyt-db`. Registrada porque o dano recai sobre um serviço que não mudou, e porque a classe do erro do banco (`28P01` vs `ECONNREFUSED`) é o que separa "banco errado" de "problema de topologia". Fonte: incidente de 2026-08-10.
- Atualizada [alias-de-servico-colide-em-rede-compartilhada.md](alias-de-servico-colide-em-rede-compartilhada.md): o deploy seguinte à renomeação falhou com `Domain ... is attached to service db which does not exist in the compose`. O Dokploy guarda o domínio atrelado ao nome do serviço fora do repositório, então renomear exige atualizar o painel no mesmo movimento; a mensagem ainda revelou que o domínio público apontava para o Postgres, não para o serviço web. Fonte: incidente de 2026-08-10.

## 2026-08-12

- Atualizada [upload-em-server-action-estoura-mudo.md](upload-em-server-action-estoura-mudo.md): mesmo com redução no cliente e `bodySizeLimit` de 12 MB, as quatro poses juntas estouravam o corpo e a tela pedia "envie em duas etapas" — um limite de transporte virando tarefa do usuário. Envio fatiado em uma Server Action por pose; registrado o que a mudança arrasta junto (action retorna erro em vez de redirecionar, consentimento marcado só na primeira chamada, retomada a partir do item que falhou). Fonte: sessão de 2026-08-12.
- Atualizada [upload-em-server-action-estoura-mudo.md](upload-em-server-action-estoura-mudo.md): segundo sintoma da mesma sessão — a mensagem de erro existia, mas no topo do formulário, fora da dobra do botão; o usuário tocou em enviar várias vezes sem ver retorno. Criado `@/components/tela/aviso-acao`, que renderiza o aviso junto à ação e o traz ao campo de visão com foco. Registrada porque aviso fora do campo de visão é indistinguível de aviso inexistente, e a correção não pode depender de cada tela lembrar disso. Fonte: sessão de 2026-08-12.
- Criada [e2e-mede-compilacao-nao-fluxo.md](e2e-mede-compilacao-nao-fluxo.md): o job de E2E levava 3m20 e a suspeita recaía sobre o navegador, mas o custo estava em `next dev` compilar cada rota na primeira visita com o cronômetro do teste correndo. Três variantes medidas com harness próprio; venceu servir o build de produção (120 s → 52 s local, 3m20 → 1m58 no CI, 27/27). Registrada porque o diagnóstico depende de ler o tempo por teste em vez do total, e porque a variante mais rápida no relógio (paralelismo) era a pior no sinal. Fonte: PR #75.

## 2026-08-13

- Criada [auditoria-visual-por-screenshot.md](auditoria-visual-por-screenshot.md): pedido para alinhar toda a aplicação aos componentes do MacroFactor. Em vez de ler arquivo a arquivo, `scripts/auditoria-visual.ts` semeou sessão e capturou as 44 telas: as imagens revelaram três desvios sistêmicos invisíveis no código (`<select>`/`<progress>` nativos com desenho do sistema operacional, um cartão por destino onde a referência usa lista de linhas, estado vazio como frase solta). Cada um virou componente em `src/components/tela/`. Registrada porque a ordem importa — renderizar antes de julgar — e porque a contagem de páginas que importam o kit prioriza o trabalho em segundos. Fonte: sessão de 2026-08-13.
- Criada [casco-de-altura-fixa-ancora-o-rodape.md](casco-de-altura-fixa-ancora-o-rodape.md): a `BottomNav` aparecia coberta pela barra do navegador no Início, na prévia do treino e no resumo. A causa não era a safe area (já tratada), e sim o casco autenticado usar `min-h-full`: a rolagem pertencia ao documento e a nav `sticky` descia com ele; com `viewportFit: "cover"` a faixa final ainda caía atrás da barra de UI. Registrada porque a correção depende de dois detalhes que se anulam em silêncio (`flex-1` mata `h-dvh`; sem `min-h-0` o `main` não encolhe) e porque a regressão só é detectável por asserção geométrica. Fonte: relato com capturas de 2026-08-13.
- Criada [servidor-de-dev-sem-teto-de-heap.md](servidor-de-dev-sem-teto-de-heap.md): o `next-server` chegou a ~5,5 GB numa sessão longa e travou a máquina por swap, sem erro algum vindo da aplicação. A causa não é vazamento pontual: o HMR retém módulos server-side ao longo da sessão e, com `NODE_OPTIONS` vazio, o V8 dimensiona o old space pela RAM total — o processo cresce até disputar memória com o resto do sistema. `scripts/dev-local.sh` passou a fixar `--max-old-space-size=2048` (sobrescrevível) e `.next/dev`, que estava com 3,5 GB, foi limpo. Registrada porque o sintoma aponta para o lugar errado e porque o tamanho de `.next/dev` é o termômetro barato do problema. Fonte: incidente de 2026-08-13.

## 2026-08-22

- Atualizada [casco-de-altura-fixa-ancora-o-rodape.md](casco-de-altura-fixa-ancora-o-rodape.md): a correção da `BottomNav` fixa revelou uma segunda rolagem em Progresso. O `ChipSelecao` usava inputs absolutos sem containing block no `main` rolável; o casco passou a ser `relative` e o input transparente passou a cobrir o chip, eliminando o excedente do documento sem perder acessibilidade. Fonte: captura local de 2026-08-22.

## 2026-08-14

- Criada [recorte-versionado-caduca-em-silencio.md](recorte-versionado-caduca-em-silencio.md): auditoria do que é enviado ao agent na geração do plano revelou planos gerados só com o Núcleo — sem medições, metas nem fotos — com `degradado=false` e `campos_omitidos=[]` na Trilha. O Recorte `plano-inicial` subira de v2 para v4 e `consentimentosVigentes` filtrava por igualdade de versão, devolvendo lista vazia: "consentiu na v2" virou "nunca consentiu". Introduzidos `classificarConsentimentos`/`estadoConsentimento` com `precisaReconsentir`, e a regeneração passou a pedir reconfirmação em vez de degradar em silêncio. Registrada porque o sintoma é qualidade de resposta, não erro, e porque a Trilha não denuncia a perda quando a versão antiga do Recorte nem declarava os campos novos. Fonte: sessão de 2026-08-14.
- Criada [e2e-trava-no-health-check-do-webserver.md](e2e-trava-no-health-check-do-webserver.md): ao validar a fatia que expõe as explicações do agent fora do onboarding, `npx playwright test` parou de produzir qualquer saída — sem lista, sem falha, sem timeout — e o diagnóstico foi por lentidão até `DEBUG=pw:test:*` revelar a última linha em `pw:webserver HTTP GET`. O bypass de sessão de dev do `proxy.ts` grava o cookie e redireciona; um cliente sem cookie jar — o health check do `webServer`, e `curl` sem `-c/-b` — recebe `307 → /inicio` para sempre (`curl -L` sai com 47). `reuseExistingServer` não protege, porque o health check roda mesmo com o servidor no ar. Rodar contra o standalone (`node .next/standalone/server.js`, não `next start`) com `AUTH_URL` casado levou o teste de travado a 4,0 s e a suíte a 58,2 s (29/29). Registrada porque suíte muda e suíte lenta têm causas distintas e o sintoma incrimina os testes. Fonte: sessão de 2026-08-15.
- Consolidado o modelo base do agent em `google/gemini-2.5-flash-lite` para todas as operações, em produção e desenvolvimento. O catálogo `:free` paralelo produziu duas falhas que só existiam localmente: um modelo sem visão numa operação cujo Recorte passou a enviar fotos (`No endpoints found that support image input`) e outro que ignorava o schema (`No object generated`). Criado `npm run ia:modalidades`, que compara os campos de imagem de cada Recorte com `input_modalities` do modelo no catálogo do OpenRouter. Fonte: sessão de 2026-08-14.

## 2026-08-15

- Criada [rotulo-de-confianca-esconde-a-origem.md](rotulo-de-confianca-esconde-a-origem.md): ao construir o registro de refeição por foto, os itens estimados pelo modelo reusaram `ROTULO_CONFIANCA` — o mapa que descreve a *fonte eleita* pela ponderação de `proveniencia.ts`, não a certeza. Type check e testes passavam (o tipo `Confianca` é o mesmo), mas a captura da tela mostrou "Peito de frango grelhado · Valor de tabela analítica" sobre um alimento que o modelo apenas reconheceu numa foto — estimativa vestida de medição, exatamente o que a user story 59 proíbe. Introduzido `rotuloDeConfianca(confianca, origem)`. Registrada porque o defeito é invisível em diff e em teste de tipo, e só aparece renderizado. Fonte: sessão de 2026-08-15.

## 2026-08-16

- Criada [status-bar-translucida-quebra-a-geometria.md](status-bar-translucida-quebra-a-geometria.md): relato com capturas do app instalado na tela de início do iPhone 16 Pro Max mostrava a barra de abas com faixa morta embaixo e os títulos das telas atrás do relógio. Medir a captura resolveu antes de qualquer hipótese: a faixa tinha ~62pt, não os 34pt do indicador de home — era o inset **superior** reaparecendo embaixo. `black-translucent` sem nenhum consumo de `env(safe-area-inset-top)` (o grep não achava um só uso) faz o iOS reportar viewport menor que a tela e ancorada em `y=0`, com todos os insets zerados. Trocado por `black`; descobertos no caminho dois defeitos vizinhos: `h-16` com `border-box` fazia o inset inferior comer o alvo tocável de 64pt para 30pt, e `env()` inline impedia qualquer teste, já que o Chromium do Playwright não implementa `Emulation.setSafeAreaInsets`. Safe areas viraram tokens `--safe-top`/`--safe-bottom` justamente para dar ao E2E um ponto de injeção. Registrada porque o sintoma inferior aponta para a causa superior, e porque o E2E `navbar-visivel` existente passava verde o tempo todo (Pixel 7, sem insets). Fonte: relato com capturas de 2026-08-16.

## 2026-08-19

- Criada [controle-escondido-perde-o-alvo-de-toque.md](controle-escondido-perde-o-alvo-de-toque.md): ao construir o `SeletorSegmentado` do descanso entre séries, o input nativo foi escondido com `sr-only` copiando o padrão do `ChipSelecao`. Os 42 testes unitários do kit passaram — `getByRole("radio")` encontra o input, que continua na árvore de acessibilidade — mas o E2E expirou em 30 s acumulando `<label ...> intercepts pointer events` a cada retry: `sr-only` encolhe o controle para 1×1px e o centro do segmento de 44px pertence ao `label`, não ao input. Trocado por `absolute inset-0 opacity-0` com `appearance-none` e `pointer-events-none` no texto, o cenário passou em 1,7 s. Registrada porque o sintoma incrimina o `label` (levando a mexer em `z-index`/`pointer-events` do contêiner), porque `fireEvent.click` no jsdom não faz hit-testing e valida um alvo errado, e porque o padrão do `ChipSelecao` é enganosamente confiável ao ser copiado para segmentos largos. Fonte: sessão de 2026-08-19.

## 2026-08-20

- Criada [watch-registrar-campo-inexistente-nunca-dispara.md](watch-registrar-campo-inexistente-nunca-dispara.md): em pelo menos 4 sessões (16–19 de agosto), watches registrados via `watch_registrar` contra a source `process` nunca disparavam porque `condition.field` referenciava um nome inexistente — `{field:"event", value:"exit"}` (a source publica `pid`/`status`/`exitCode`/`signal`/`command`, nunca `event`) ou `{field:"status", in:["completed","failed"]}` (`status` só assume `"running"`/`"exited"`). O motor de condições tratava campo ausente como `undefined`, sempre `false`, sem distinguir "ainda não satisfeita" de "estruturalmente impossível"; o agente encerrava o turno confiando num wake que nunca vinha. Corrigido na extensão `~/.pi/agent/extensions/watch` com aliases universais (`event`/`type`/`resource` derivados do envelope do evento, em `core/aliases.ts`) e aviso não bloqueante de campo desconhecido (`core/field-discovery.ts`, com sugestão por distância de Levenshtein). Registrada porque o padrão já se repetiu 4 vezes e muda como condições de `watch_registrar` devem ser escritas e revisadas daqui pra frente. Fonte: relatório de incidente de 2026-08-19.
- Criada [ferramentas-ai-sdk-inputschema.md](ferramentas-ai-sdk-inputschema.md): ao dar ao agent de planejamento a primeira Ferramenta de Leitura (consulta à ExerciseDB), o `tool()` do AI SDK v7 exigiu `inputSchema` (não `parameters`, como nos exemplos antigos) e `execute` em overload próprio; `dynamicTool()` não aceita esse schema e força `unknown`; e chamar `execute` direto não aplica o `default()` do zod, exigindo fallback explícito. Registrada porque o TypeScript apresenta o erro como overload opaco, porque o `decidir.ts` já suportava ferramentas sem nenhuma operação usar, e porque a auditoria (`ferramentasConsultadas`) já é automática. Fonte: sessão de 2026-08-20.

## 2026-08-21

- Criada [instrucao-nao-garante-ferramenta.md](instrucao-nao-garante-ferramenta.md): investigando a lentidão da geração de plano (17.363 tokens de entrada, 10.332 de saída, 9,1 s até o primeiro token, ~62 s no total), a Trilha de Decisão mostrou 19–22 chamadas a `consultarExercicio` por geração — e o MD5 dos 20 `comoExecutar` devolvidos batia byte a byte com o catálogo estático. A instrução mandava buscar na ExerciseDB e traduzir; sem nenhum campo de schema capaz de distinguir "consultou" de "copiou o prompt", o modelo copiou. Corrigido dividindo `plano-inicial` em `plano-treino` e `plano-nutricao` (geradas em paralelo, recortes e consentimentos próprios), removendo `consultarExercicio`, tirando `comoExecutar` e `justificativa` do schema (ambos vêm do catálogo; a tela mostra `explicacao.porque`) e trocando o catálogo JSON por linhas (13.409 → 2.982 chars). Instrução de 17.176 chars → 5.089 na maior das duas. Registrada porque prompt grande e laço de tool calling têm curas diferentes, e o laço dominava o TTFT. Fonte: sessão de 2026-08-21.
- Criada [downgrade-de-dependencia-quebra-o-npm-run-dev.md](downgrade-de-dependencia-quebra-o-npm-run-dev.md): `npm run dev` abortava em "aplicando migrações" com `error: unknown command 'migrate'`. O `dev-local.sh` e o `drizzle.config.ts` estavam intactos; a causa era um downgrade não commitado de `drizzle-kit` (`^0.31.10` → `^0.18.1`) com `package-lock.json` regenerado junto, e o subcomando `migrate` só existe a partir da 0.20. Restaurado com `git checkout -- package.json package-lock.json && npm ci`; migrações aplicadas com sucesso. Adicionado `verificar_drizzle_kit` ao passo `migrar` do `scripts/dev-local.sh`, que falha cedo com a instrução de restauração. Registrada porque o sintoma incrimina o script e o lockfile regenerado faz `npm ci` reproduzir o ambiente quebrado de forma consistente. Fonte: sessão de 2026-08-21.

## 2026-08-22

- Criada [galeria-compila-mas-nao-renderiza.md](galeria-compila-mas-nao-renderiza.md): ao substituir a rota `/design` pelo Storybook 10 (`@storybook/nextjs-vite`), `storybook build` aprovou as 118 stories com exit code 0 — mas 2 delas (`EnvioFotos`) renderizavam `#storybook-root` com altura 0, porque `useRouter` lança `invariant expected app router to be mounted` e o Storybook captura a exceção no error boundary, desenhando-a como conteúdo da moldura. Corrigido com `parameters.nextjs.appDirectory = true` global em `.storybook/preview.tsx` (e não por story), mais `scripts/verificar-stories.ts` / `npm run storybook:verificar`, que abre cada story em Chromium 390×844 e reprova por erro de página **ou** altura < 4px. Ligado ao job `estatica` do CI, não a um job novo, porque o ruleset da `main` exige exatamente 6 checks pelo nome. Registrada porque build e render são portões distintos numa galeria de componentes, e o código de saída deixa de ser evidência quando a ferramenta desenha o próprio erro na tela. Fonte: sessão de 2026-08-22.

## 2026-08-23

- Criada [check-exigido-com-paths-trava-o-merge.md](check-exigido-com-paths-trava-o-merge.md): a esteira levava ~8 min e a suspeita recaía sobre o E2E, mas a cronometragem por passo mostrou o E2E em 1 min 11 s e o passo `Renderização das stories` (54 stories em Chromium, dentro do job `estatica`) em 4 min 16 s — com `e2e` declarando `needs: [estatica, unidade, integracao, build]`, o único portão que exercita o produto começava no minuto 6. Corrigido reduzindo o `needs` do E2E a `[build]` (única dependência material; em `pull_request` os jobs baratos rodam de qualquer forma e um vermelho já reprova) e movendo galeria e `npm audit` para workflows próprios com `schedule` + `workflow_dispatch` + `pull_request` filtrado por `paths`. Ao fazer isso, `Auditoria de dependências` teve de sair de `required_status_checks` do ruleset 20077396: um context exigido cujo job não roda deixa o PR pendente para sempre, sem ficar vermelho. Fonte: sessão de 2026-08-23.

## 2026-08-24

- Criada [componente-dentro-da-rota-escapa-da-governanca.md](componente-dentro-da-rota-escapa-da-governanca.md): ao conferir de que componentes a aba Diário era feita, apareceu que o painel de macros vivia em `src/app/(app)/diario/painel-macros.tsx` e os três cartões da linha do tempo eram JSX cru dentro de uma função `cartao()` no `page.tsx` — nenhum com story, teste de contrato ou entrada em `ui_catalogo`, com `npm run ui:verificar` e `storybook build` verdes o tempo todo. O portão não falhava: `lerCatalogo` varre `src/components/**`, e o que nasce em `src/app/**` nunca entra na lista fiscalizada. Corrigido extraindo a tela para `src/components/diario/` (`PainelMacrosDia`, `NavegacaoDia`, `AcoesRegistro`, `CartaoSessaoDiario`, `CartaoConsumo`, `CartaoRefeicaoPlanejada`, `LinhaDoTempoDiario`), com os `<form>` de server action entrando por slot para que os cartões renderizem no Storybook; 5 stories e 11 testes de contrato passaram a ser exigidos. Adicionada a checagem `componentesForaDoCatalogo`, que reprova todo `.tsx` de `src/app/**` fora das convenções de rota do Next que exporte um componente, com os 15 casos herdados numa lista explícita que só encolhe; `inicio`, `sessao` e `diario` entraram nas camadas com teste de contrato. Fonte: sessão de 2026-08-24.
- Criada [e2e-flaky-sorteia-cenarios-diferentes.md](e2e-flaky-sorteia-cenarios-diferentes.md): no PR #145, o job `E2E mobile` reprovou com 3 falhas em telas fora do escopo da mudança (`avaliacao-corporal`, `mudanca-objetivo`, `revisao-corporal`), com a `main`-base verde 4 h antes — o que aparentemente incriminava o commit. Três execuções do mesmo job sem alteração de código deram 3, 1 e 0 falhas, sem repetir cenário: regressão é determinística, vítima sorteada é corrida. A falha da 2ª tentativa expôs o mecanismo (`Expected /triagem/altura, Received /triagem/sexo`): `page.goto` disparado logo após "Continuar", sem esperar a server action gravar. Registrada porque duas comparações mal construídas custaram tempo — 3 arquivos na branch contra 1 na `main`, e `next dev` local contra `next start` do CI — e porque `--repeat-each=3` converteu impressão em evidência (falha em 8,1 s; 3/3 passando em 2–3 s). Fonte: sessão de 2026-08-24.

## 2026-08-27

- Criada [defaultvalue-key-identidade-dominio.md](defaultvalue-key-identidade-dominio.md): na Sessão de Treino, o servidor devolvia a referência correta do exercício seguinte, mas os inputs continuavam mostrando o exercício anterior porque `serie.numero` era reutilizado como `key` e `defaultValue` não atualiza um controle já montado. Registrada a separação entre identidade React e rascunho efêmero, além da evidência diagnóstica em que o atributo HTML dizia `70` e a propriedade visível do input permanecia `40`. Fonte: correção da referência histórica por exercício e série.
- Atualizadas [e2e-flaky-sorteia-cenarios-diferentes.md](e2e-flaky-sorteia-cenarios-diferentes.md) e [e2e-auth-url-local.md](e2e-auth-url-local.md): no PR #151, `E2E mobile` reprovou duas vezes em `revisao-corporal` (`Scorecard de Progresso` não encontrado), mas o `error-context.md` do artefato mostrava a página ainda em `/progresso/revisao` com o botão "Iniciar revisão" visível — a server action `gerarRevisaoSemanal` (seis consultas mais escrita antes do `redirect`) não cabia nos 5 s padrão do `expect`, enquanto localmente levava 1,4 s no mesmo modo do CI (`next start`). Elevado o padrão para `expect: { timeout: 15_000 }` em `playwright.config.ts`, com remoção dos quatro `{ timeout: 15_000 }` avulsos que já compensavam o mesmo aperto. No caminho, rodar com `PLAYWRIGHT_BASE_URL=http://localhost:3100` derrubou os três cenários de `e2e/acesso` com `chrome-error://chromewebdata/`: a variável `AUTH_URL` do ambiente local seguia na 3000, e o comentário do config prometia que trocar a porta bastava. O `webServer` passou a derivar `AUTH_URL` do `baseURL`, eliminando a pegadinha em vez de documentá-la. Fonte: sessão de 2026-08-27.

## 2026-08-28

- Criada [e2e-reusa-o-dev-server-que-o-agente-deixou-no-ar.md](e2e-reusa-o-dev-server-que-o-agente-deixou-no-ar.md): na separação das abas Dieta/Treino, a suíte reprovou com 6 falhas espalhadas por arquivos sem relação entre si (`diario`, `fotos-r2`, `registro-por-foto`, `sessao` ×2, `substituicao`), todas por timeout de clique, com console limpo e a tela renderizada corretamente no snapshot. O denominador comum estava no log do Playwright: `<nextjs-portal> … intercepts pointer events`. Eu havia deixado `npm run dev` no ar para inspeção visual e o `reuseExistingServer: true` do `playwright.config.ts` fez a suíte rodar contra o dev server em vez do `next start` que ela declara. O overlay é ancorado num canto inferior — exatamente onde a `BottomNav` põe a primeira e a última aba — e a mudança de `/inicio` para `/dieta` na primeira posição expôs a oclusão que antes ninguém tocava. Registrada também a correção errada que tentei antes: mover `devIndicators.position` para `bottom-right` apenas transferiu o problema de Dieta para Mais, porque numa barra de largura total nenhum canto é seguro. Encerrado o dev server e revertida a config, a mesma suíte fechou 40/40 contra produção — inclusive `registro-por-foto`, que a bissecção com `git stash` havia classificado como falha pré-existente porque ambas as execuções usaram o mesmo dev server. Fonte: sessão de 2026-08-28.
- Atualizada [servidor-de-dev-sem-teto-de-heap.md](servidor-de-dev-sem-teto-de-heap.md): o incidente de 2026-08-13 se repetiu com a correção da época ativa — `next-server` em 4,4 GB de `RssAnon` com `--max-old-space-size=2048` não violado, 4,6 min de processo, swap da máquina em 1,8 GB e `.next` em 2,8 GB. A causa registrada estava incompleta: desde que `turbopack: {}` entrou em `next.config.ts` (2026-07-24), o `next dev` compila em Rust via N-API e mantém o grafo em memória nativa, fora do heap do V8, então a flag governava um alocador que deixara de ser o dominante e apenas *aparentava* proteger. Agravante do dia: a suíte E2E rodava contra o `next dev`, varrendo dezenas das 55+ rotas em minutos. Corrigido movendo o limite para uma camada que o processo não contorna — `scripts/dev-local.sh` sobe o `next dev` sob `systemd-run --user --scope` com `MemoryMax=4G`, `MemoryHigh=3G` e `MemorySwapMax=0`, com fallback onde não houver systemd de usuário; verificado lendo `memory.max`/`memory.high`/`memory.swap.max` de dentro do scope e reproduzindo o estouro com `MemoryMax=200M` (SIGKILL, exit 137, sem swap). O padrão de `playwright.config.ts` passou de `next dev` para `next start`, alinhando local e CI, com guard de `.next/BUILD_ID` que falha cedo em vez de virar timeout de health check. `npm run dev` passou a avisar quando `.next/dev` excede 1 GB. Fonte: sessão de 2026-08-28.

## 2026-08-29

- Atualizada [e2e-flaky-sorteia-cenarios-diferentes.md](e2e-flaky-sorteia-cenarios-diferentes.md): o job `E2E mobile` reprovou em `mudanca-objetivo` esperando `Experimento ativo`, e a investigação desceu até `ativarExperimentoPlano` antes de notar o que o próprio stack trace dizia — `page.goto("/inicio")` na linha 52, rota removida nesta mesma entrega, e sem a âncora do scorecard que a árvore local já tinha. O CI rodou `2f06a46`; todas as correções de E2E estavam como modificação local não commitada. Registrada a checagem que resolveria em um comando: quando o stack do CI aponta linha ou rota inexistente na árvore, o commit sob teste é outro. No caminho, o loop custou três obstáculos de ambiente — `next start` não serve o build com `output: standalone` (o CI usa esse comando e passa, então a config não foi alterada), o timeout de 30 s do `webServer` não cobre o boot do `next dev`, e o Postgres de desenvolvimento estava fora do ar (`/api/saude` em 503). Adicionada a âncora faltante entre `Ver evidências` e `Ver proposta`, e a equivalente após `Filtrar sugestões` em `substituicao`; 5/5 verdes em ~3,8 s. Fonte: sessão de 2026-08-29.
- Criada [service-worker-serve-rsc-velho-apos-server-action.md](service-worker-serve-rsc-velho-apos-server-action.md): `E2E mobile` reprovava nas *mesmas* duas vítimas (`mudanca-objetivo` e `revisao-corporal`) em execuções sucessivas — padrão de regressão, não da corrida que a leitura anterior assumiu ao adicionar âncoras de navegação. O `error-context.md` mostrava estado impossível para corrida: `POST 303` da Server Action seguido da tela de destino ainda no passo anterior (formulário "Ativar Experimento de Plano" montado depois da ativação; proposta `pendente` depois de criar o rascunho). Duas causas independentes com o mesmo sintoma. (1) O `defaultCache` do `@serwist/next` fecha com `NetworkFirst` sobre `pages-rsc-prefetch`/`pages-rsc`/`pages`, guardando 24 h qualquer resposta same-origin; o `<Link>` prefetcha a etapa seguinte enquanto a anterior está na tela, então o RSC gravado é anterior à escrita. Invisível em `next dev`, porque o SW só registra com `NODE_ENV === "production"` — apareceu quando a suíte passou a rodar contra `next start`. (2) As cinco actions de `progresso/revisao` revalidavam rota diferente da que redirecionavam (`/treino` e `/progresso` antes de ir para `.../experimento`), deixando o destino intacto no Router Cache. Corrigido com `NetworkOnly` restrito a `Next-Router-Prefetch === "1"` e `revalidatePath("/progresso/revisao", "layout")`. Registrado também o erro do caminho: a primeira versão do matcher pegava toda navegação e todo RSC, o que corrigiu a Revisão Semanal e derrubou `offline.e2e.test.ts` — a navegação sem rede depende do `NetworkFirst` que eu havia removido. Suíte fechou 40/40 (antes 35/2/3) e os três cenários passaram 9/9 com `--repeat-each=3`, sem alterar nenhum arquivo de teste. Fonte: sessão de 2026-08-29.

## 2026-08-30

- Criada [formato-de-audio-morre-no-cliente-do-provedor.md](formato-de-audio-morre-no-cliente-do-provedor.md): a transcrição por áudio continuava falhando em produção e também no `npm run start` local, com `AI_UnsupportedFunctionalityError: 'audio media type audio/ogg'` — depois de duas correções que assumiram recusa do endpoint (#159 removeu `audio/mp4` da lista aceita; #160 passou a converter MP4 para OGG/Opus com FFmpeg). A premissa era errada: a exceção é lançada por `convertToOpenAICompatibleChatMessages` ao traduzir a parte `file` para `input_audio`, antes de a requisição sair da máquina, e `getAudioFormat` do `@ai-sdk/openai-compatible` mapeia apenas `audio/wav` e `audio/mpeg`. Ou seja, o OGG de destino da correção anterior estava tão recusado quanto o MP4 de origem, e o WebM do Chrome/Firefox — caminho da maioria dos usuários — nunca funcionou. Diagnosticado com um loop que chama o `doGenerate` real contra `http://127.0.0.1:1/v1`, onde "bad port" prova que a conversão passou e `UnsupportedFunctionalityError` reproduz o erro de produção sem gastar crédito. Corrigido separando as duas perguntas em duas listas (`TIPOS_AUDIO_REFEICAO`, o que aceitamos receber, agora com `audio/mp4` de volta; `TIPOS_AUDIO_PROVEDOR`, o que conseguimos enviar) e transcodificando tudo que não for aceito para MP3 mono 16 kHz — a extensão de entrada deixou de ser declarada ao FFmpeg, que detecta o container pelo conteúdo, então a mesma chamada serve para WebM e MP4. Adicionado `audio-provedor.unit.test.ts`, que trava a lista contra o conversor real do SDK: nem mock nem teste de integração com o OpenRouter pegariam essa falha, porque ela acontece antes da chamada. Verificado com áudio real nos dois containers de navegador (WebM e MP4 → `audio/mpeg`, conversão aceita), 173 testes de unidade verdes, `lint` e `typecheck` limpos. Fonte: sessão de 2026-08-30.
- Criada [campo-controlado-nao-normaliza-durante-a-digitacao.md](campo-controlado-nao-normaliza-durante-a-digitacao.md): o atleta relatou não conseguir corrigir "Refrigerante de cola" para "Coca cola zero" na revisão da estimativa — as letras entravam, os espaços não, e o texto saía "Cocacolazero". `renomearItem` fazia `nome.trim()` e o input é controlado pela descrição do item, então cada espaço era apagado no mesmo keystroke em que era digitado; nenhum alimento de mais de uma palavra podia ser corrigido. A normalização já existia onde devia (`registrarConsumoRealAction`, antes de persistir), então o `trim` do meio da digitação não protegia nada. Registrada principalmente pelo teste que não pegou: "corrigir o alimento preserva os macros" usava `fireEvent.change` com o valor completo, que entrega o texto pronto e nunca produz o estado intermediário onde o defeito vive — testa colagem, não digitação. Os dois testes de regressão adicionados (domínio e componente, este com `userEvent.type` e `rerender` reencaminhando o estado) foram verificados nos dois sentidos: 2 falhas ao reintroduzir o `trim`, 101/101 com a correção, enquanto o teste antigo permanece verde nas duas versões. Fonte: sessão de 2026-08-30.
- Atualizada [e2e-reusa-o-dev-server-que-o-agente-deixou-no-ar.md](e2e-reusa-o-dev-server-que-o-agente-deixou-no-ar.md): a armadilha de 2026-08-28 reapareceu com um servidor de **produção** na porta, e não um `next dev` — `npm run app:up`, que eu mesmo havia subido para o usuário conferir a tela. Os 3 cenários de `registro-retroativo` deram timeout clicando em "Estimar calorias e macros" com o botão `disabled`, sem nenhum sintoma de overlay: o `playwright.config.ts` sobe o servidor com `OPENROUTER_BASE_URL` apontando para o dublê de IA da porta 4311, e o servidor do `app:up` não tem essa variável — ele falava com o OpenRouter real e a estimativa nunca voltava. O sinal que separou ambiente de regressão foi 3 de 3 falhando, incluindo os dois cenários que a mudança não tocava; a confirmação veio de `tr '\0' '\n' < /proc/<pid>/environ | grep OPENROUTER_BASE_URL`, vazio. Generalizada a regra para "`reuseExistingServer` só é seguro quando o processo na porta foi subido pelo próprio Playwright", registrado o comando de verificação do ambiente do processo, e anotado que `npm run app:down` derruba o Postgres junto (a sequência correta é `app:down && db:up`). Liberada a porta, os mesmos 3 cenários passaram em 6,3 s e a suíte completa fechou 43/43 sem alterar código. Fonte: sessão de 2026-08-30.
